name: Move agent docs into README

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  move-docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Move guide into README
        run: echo ready
